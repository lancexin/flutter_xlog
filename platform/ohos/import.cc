/**
 * Stub translation unit so that the marsxlog SHARED library has a non-empty
 * source list. The real entry points come from the linked xlog static library
 * (Kmp_*, ExportXlog, Dart_XloggerWrite).
 */

void ImportLibs() {
}
